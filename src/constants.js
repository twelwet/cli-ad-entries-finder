'use strict';

const ErrorMessage = {
  SOMETHING_WENT_WRONG: 'Something went wrong.',
};

const ExitCode = {
  SUCCESS: 0,
  FAILURE: 1
};

const FILE_NAME = `entries.json`;

module.exports = { ErrorMessage, ExitCode, FILE_NAME };
